<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotelAppGuestWin10.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:HotelAppGuestWin10.ViewModel"
    x:Class="HotelAppGuestWin10.View.GuestView"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:GuestViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="586" Margin="197,78,0,0" VerticalAlignment="Top" Width="321" SelectedItem="{Binding SingletonViewModel.SelectedGuest, Mode=TwoWay}" ItemsSource="{Binding SingletonViewModel.GuestList}" Foreground="{x:Null}" Background="#FF14BA58" CanDragItems="True" CanReorderItems="True"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="204,29,0,0" TextWrapping="Wrap" Text="Gæster" VerticalAlignment="Top" Height="43" Width="370" FontSize="36" FontFamily="Source Code Pro Semibold"/>
        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Height="44" Margin="604,116,0,0" TextWrapping="Wrap" Text="{Binding SingletonViewModel.SelectedGuest}" VerticalAlignment="Top" Width="498"/>
    </Grid>
</Page>
